import React, { Component } from 'react';
const WrapperContentCenter = (props: any) => (
    <div className={'app__content-center'}>
        {props.element}
    </div>
);

export default WrapperContentCenter;